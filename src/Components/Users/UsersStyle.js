import styled from 'styled-components';

const UsersStyle = WrappedComponent => styled(WrappedComponent)`
  position:relative;
  margin-top:100px;
  margin-left:20px;
  display:inline-flex;
  justify-content:center;
  flex-wrap:wrap;

`;
export default UsersStyle;
